const users = require('./users');
const posts = require('./posts');
const comments = require('./comments');
const votes = require('./votes');
const pages = require('./pages');

module.exports = {
  users, posts, comments, votes, pages,
};
